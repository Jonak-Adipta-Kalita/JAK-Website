import Header from "@components/Header";

const Home = async () => {
    return (
        <div className="h-screen bg-bg-color-dark text-text-color-dark">
            <Header />
        </div>
    );
};

export default Home;
