const ShowcaseCard = ({
    name,
    imageURL,
    messages,
}: {
    name: string;
    imageURL: string;
    messages: string[];
}) => {
    return <div className=""></div>;
};

export default ShowcaseCard;

// TODO
